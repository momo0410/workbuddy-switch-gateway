# Trellis: 已在 2026-09-12 整理中移除原 union 规则。
#
# 原规则针对 `.trellis/workspace/*/journal-*.md`，但该目录从未存在于本仓库
# （`workflow.md` / `workspace/` 均未创建），规则永不匹配任何文件。
#
# 原本注释引用的 `.trellis/spec/cli/backend/directory-structure.md` 同样不存在。
#
# 若将来重新引入 Trellis 的 workspace 日志机制，再按需加回：
# .trellis/workspace/*/journal-*.md merge=union
